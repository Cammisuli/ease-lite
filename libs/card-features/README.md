# card-features

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build card-features` to build the library.

## Running unit tests

Run `nx test card-features` to execute the unit tests via [Vitest](https://vitest.dev/).
