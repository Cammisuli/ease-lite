# bank-features

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bank-features` to build the library.

## Running unit tests

Run `nx test bank-features` to execute the unit tests via [Vitest](https://vitest.dev/).
